<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.callin.dao.UserMapper">
	<select id="getUserList" resultType="UserDto">
		SELECT 
			  m_id			AS userId
			, m_pw			AS userPw
			, m_name		AS userName
			, m_email		AS userEmail
			, m_addr		AS userAddr
			, m_reg_date	AS userRegDate
			, m_class		AS userClass
		FROM 
			tb_member
	</select>
	<select id="getUserRead" resultType="UserDto" parameterType="String">
		SELECT 
			  m_id			AS userId
			, m_pw			AS userPw
			, m_name		AS userName
			, m_email		AS userEmail
			, m_addr		AS userAddr
			, m_reg_date	AS userRegDate
			, m_class		AS userClass
		FROM 
			tb_member
		WHERE
			m_id = #{userId}
	</select>
</mapper>